import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const HardwareScreen = (props: Props) => {
  return (
    <View>
      <Text>HardwareScreen</Text>
    </View>
  );
};

export default HardwareScreen;

const styles = StyleSheet.create({});
